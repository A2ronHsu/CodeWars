//https://www.codewars.com/kata/525f3eda17c7cd9f9e000b39/train/javascript

const zero = (fn) => { fn(0)}
const one = (fn) => { fn(1)}
const two = (fn) => { fn(2)}
const three = (fn) => { fn(3)}
const four = (fn) => { fn(4)}
const five = (fn) => { fn(5)}
const six = (fn) => { fn(6)}
const seven = (fn) => { fn(7)}
const eigh = (fn) =>  {fn(8)}
const nine = (fn) => { fn(9)}

function plus() {}
function minus() {}
function times() {}
function dividedBy() {}